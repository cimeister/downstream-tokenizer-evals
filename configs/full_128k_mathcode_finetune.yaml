# Finetune existing 1B models on math+code data.
# Same d24 architecture, 5B tokens of 50/50 math+code.
# Resumed from existing full-128k checkpoints.
# ~4,768 steps at 1M batch size, ~2.5h wall time (single node 4×GH200).

name: full-128k-mathcode-ft

model:
  n_layer: 24
  n_embd: 1536
  n_kv_head: 12
  sequence_len: 2048
  window_pattern: "SSSL"
  ve_dim: 128

training:
  target_param_data_ratio: -1   # fixed token budget
  max_tokens: 5000000000        # 5B tokens
  num_iterations: -1            # auto-compute from max_tokens
  total_batch_size: 1048576
  device_batch_size: 16
  warmup_steps: 40
  warmdown_ratio: 0.65
  final_lr_frac: 0.05
  eval_tokens: 41943040
  compile: true

optimizer:
  matrix_lr: 0.02
  embedding_lr: 0.3
  unembedding_lr: 0.008
  scalar_lr: 0.5
  weight_decay: 0.28
  width_lr_exponent: -1.0

data:
  data_dir: null

logging:
  eval_every: 500
  save_every: 500
  max_checkpoints: 0

wandb:
  enabled: true
  project: tokenizer-lm
  run_name: null

output_dir: /capstor/scratch/cscs/${USER}/tokenizer-lm/checkpoints

seed: 42
